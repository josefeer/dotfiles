#!/usr/bin/env zsh

OS=$(uname -s)
echo "Installing Binaries in $OS (TODO)"

# TODO:
# if OS == Darwin (Mac OS)
#   Verify if homebrew exists || install homebrew
#   Install binaries (brew install or Brewfile)
# elif OS == Linux (Ubuntu)
#   Update apt and add necesary repositories
#   Install binaries (apt install)

# TODO: Veryfy if Jetbrains Mono Nerd fonts is installed || install fonts https://github.com/ryanoasis/nerd-fonts/releases/download/v3.0.2/JetBrainsMono.zip
